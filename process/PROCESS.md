# PROCESS — theo dõi tiến trình triển khai

File này theo dõi tiến độ implement Navtrack theo phase. **Mỗi khi hoàn thành một phase, Claude cập nhật bảng trạng thái dưới đây** và tick tiêu chí hoàn thành trong file `phase-x.md` tương ứng.

Trạng thái: ⬜ Chưa bắt đầu · 🟨 Đang làm · ✅ Hoàn thành

| Phase | Tên | Trạng thái | Chi tiết |
|---|---|---|---|
| 1 | Nền tảng + đăng nhập + nhập vị thế | ✅ | [phase-1.md](./phase-1.md) |
| 2 | Lõi XIRR + giá tự động | ✅ | [phase-2.md](./phase-2.md) |
| 3 | Snapshot tự động | ✅ | [phase-3.md](./phase-3.md) |
| 4 | Cổ tức | ✅ | [phase-4.md](./phase-4.md) |
| 5 | Thuế bán (áp dụng) | ✅ | [phase-5.md](./phase-5.md) |
| 6 | Biểu đồ + hoàn thiện dashboard | ✅ | [phase-6.md](./phase-6.md) |
| 7 | Trái tức (lãi trái phiếu) | ✅ | [phase-7.md](./phase-7.md) |
| 8 | Lịch dòng tiền sắp tới | ⬜ | [phase-8.md](./phase-8.md) |

## Cách dùng
- Bắt đầu một phase → đổi trạng thái sang 🟨, đọc `phase-x.md` **và [`DECISION.md`](./DECISION.md)** (index quyết định đã chốt ở phase trước; mở file chi tiết trong [`decisions/`](./decisions/) khi phase chạm đúng chủ đề).
- Hoàn thành → đổi ✅, tick hết tiêu chí trong `phase-x.md`, ghi ngày hoàn thành.
- Nếu phạm vi phase thay đổi, cập nhật cả `docs/03-roadmap.md` lẫn `phase-x.md` cho nhất quán.

## Nhật ký

Ghi ngắn gọn **đã làm gì** — 1 dòng/lần. Quyết định quan trọng kèm lý do ghi ở [`decisions/`](./decisions/) (index: [`DECISION.md`](./DECISION.md)), không lặp lại chi tiết ở đây.

- 2026-07-09: Phase 1 — nền tảng: Auth.js Google OAuth + allowlist + PWA + nhập vị thế + CRUD mua/bán + unit test + e2e.
- 2026-07-10: Phase 1 — UI mockup + tách route `/holdings/closed` + materialize `quantity`/`avgCost`.
- 2026-07-11: Session regression — không phải bug.
- 2026-07-11: **Phase 1 hoàn thành** ✅.
- 2026-07-12: Phase 2 — valuation lib + NavOverride + cutoff selection + batched pricing.
- 2026-07-13: Phase 2 — wire NAV/XIRR/PnL + job Python price-fetcher.
- 2026-07-13: **Phase 2 hoàn thành** ✅.
- 2026-07-14: Đổi rule ưu tiên giá: so date giữa NavOverride vs PriceQuote; e2e DB riêng ephemeral.
- 2026-07-14: Phase 3 — issue #34: dedup constraint cho Snapshot (2 partial unique index).
- 2026-07-14: Phase 3 — issue #36: job Python snapshot-cron + GitHub Actions.
- 2026-07-15: Phase 3 — integration test cho snapshot-cron + price-fetcher.
- 2026-07-15: Phase 3 — issue #35: Presentational "Chốt số liệu hôm nay" + 6 màn Phase 3.
- 2026-07-15: Phase 3 — issue #37: Snapshot thủ công + freezeManualSnapshot + trigger tự động + thêm updatedAt.
- 2026-07-15: Phase 3 — issue #37 e2e verify + issue #46: getSnapshotHistory/Detail + comparison + e2e verify.
- 2026-07-15: **Phase 3 hoàn thành** ✅.
- 2026-07-16: Phase 4 — issue #51 verify: Presentational cổ tức khớp domain + e2e sạch.
- 2026-07-16: Phase 4 — issue #52: Setting DIVIDEND_TAX_RATE/PAR_VALUE + recordDividend + getOpenHoldings + getDividendHistory.
- 2026-07-16: Phase 4 — issue #52 verify: công thức/cache/wiring khớp + test sạch. **Phase 4 hoàn thành** ✅.
- 2026-07-16: fix(dividends) issue #52: floor stockQuantity + override tolerance 2.
- 2026-07-16: Thêm **Phase 7 — Trái tức** vào roadmap + chia 3 issue.
- 2026-07-16: fix issue #59: hàm mới xử lý cả cổ tức cổ phiếu (`derivePositionIncludingStockDividends()`, `lib/cost-basis.ts`, sau đổi tên thành `derivePosition()`) thay `derivePosition()` cũ ở 4 action mua/bán + `getHoldingDetail()` — cổ tức cổ phiếu không còn bị ghi đè mất khi có giao dịch sau đó, bán vượt cashflow-only nhưng hợp lệ nhờ cổ tức không còn bị chặn nhầm. `buildQuantityTimeline()` chuyển từ `features/dividends/` ra `lib/position-trail.ts` (dùng chung).
- 2026-07-16: fix(dashboard) issue #54: FAB "Mua/Bán" mở TransactionHoldingPicker thay vì trỏ /holdings.
- 2026-07-17: verify issue #61 (follow-up Phase 4): `recordDividend` tự bù pha loãng NAV qua `NavOverride` + `Dividend.paymentDate` — khớp spec, unit/lint/typecheck sạch; bổ sung e2e case STOCK-không-tick + chuyển case tick sang tương tác checkbox thật; `pnpm e2e` không tự chạy được trong sandbox này (không có Docker, tải Playwright browser bị egress policy chặn).
- 2026-07-17: review PR #62 (issue #61) — fix finding #2 (note trên NavOverride tự tạo), #3 (validate `paymentDate >= date`), #4 (e2e ghi cổ tức 2 lần cùng ngày), sửa mô tả PR khớp diff thật (finding #1).
- 2026-07-17: đóng quyết định treo từ #52 — ghi cổ tức KHÔNG tự trigger `Snapshot{period: MANUAL}` (lý do: cơ chế bù pha loãng NAV giữ NAV gần như liên tục qua sự kiện chia cổ tức, khác mua/bán).
- 2026-07-17: thảo luận nghiệp vụ Phase 5 (thuế bán) — chốt SELL prefill+sửa tay, BUY bỏ field thuế, `SALE_TAX_GOLD = 0`; để ngỏ đáo hạn trái phiếu (dời Phase 7) + hành vi sửa SELL đã ghi.
- 2026-07-17: thêm tính năng "Chi phí ăn mòn" vào Phase 5 — tổng thuế + phí luỹ kế (thuế bán + thuế cổ tức + phí), % trên `totalInvested`, dòng phụ dưới card lãi/lỗ.
- 2026-07-17: thêm "Cảnh báo tập trung" vào Phase 6 (`Setting{CONCENTRATION_WARNING_THRESHOLD}` = 30%, theo từng Holding) và **Phase 8 mới — Lịch dòng tiền sắp tới** (đáo hạn + coupon trái phiếu, phụ thuộc Phase 7); đảo quyết định treo Phase 7 (1): mệnh giá/coupon rate lưu cố định trên `Holding` thay vì nhập tay mỗi lần.
- 2026-07-17: sửa A1 — "Chi phí ăn mòn" đổi mẫu số từ `totalInvested` (vốn ròng, vỡ khi bán nhiều) sang `grossInvested` (vốn gộp `Σ|BUY.amount|`); xem `DECISION.md` (6).
- 2026-07-17: rà nghiệp vụ tài chính — sửa docs A2 (treo cảnh báo tập trung khi NAV danh mục khuyết giá); log #65 (C1 timing cổ tức), #66 (C2 phí mua vào avgCost), #67 (B1 realized/unrealized) để sửa sau; B2 benchmark giữ ở Backlog. Xem `DECISION.md` (7).
- 2026-07-18: `design-fetcher` kéo mockup Phase 5 thật (`Phase 5 Screens.dc.html`, 6 màn 5a-5f), sinh digest `process/UI_phase_5.md`; đối chiếu với user — chốt tính lại thuế khi sửa ngày SELL đã ghi, giữ sheet chi tiết chi phí ăn mòn, ghi rõ cấu trúc lại `ReturnMetrics` theo mockup. Xem `DECISION.md` 2026-07-18 (2). Phát hiện + log riêng footgun hạ tầng: `DesignSync` (deferred tool) không lan xuống subagent — xem `DECISION.md` 2026-07-18 (1).
- 2026-07-19: Phase 5 — verify: 12 key `Setting` (`SALE_TAX_*`/`TRANSACTION_FEE_*`) seed đúng, `avgCost` gộp phí mua (issue #66 đóng), sửa SELL đã ghi tính lại thuế/phí đúng effective dating, sheet "chi phí ăn mòn" breakdown 3 nguồn đúng %, e2e `tax-and-fee.spec.ts` đối chiếu tay khớp domain doc; bổ sung unit test thiếu (`schemas.test.ts` — guard server-side BUY không có `taxAmount`, trước đó chưa có test dù đã có refine). Log 1 gap nhỏ (client prefill không phân biệt "0 hợp lệ" vs "thiếu cấu hình" khi ngày < 2020-01-01 — hiếm, không chặn). **Phase 5 hoàn thành** ✅.
- 2026-07-21: rà spec Phase 6 dưới góc nhìn chuyên gia tài chính (chưa code) — thu hẹp A2 với materiality `MISSING_PRICE` (chỉ treo cảnh báo toàn danh mục khi thiếu giá chiếm >5% ước lượng), thêm ghi chú "tập trung tự nhiên do ít mã" (tự tính, không hard-code số mã), hysteresis buffer 3 điểm % chống nhấp nháy badge, chú thích liên kết biểu đồ phân bổ ↔ badge cảnh báo; làm rõ tường minh cách gộp XIRR toàn danh mục vào domain doc (đã đúng trong code, chỉ thiếu ghi rõ). Xem `DECISION.md` 2026-07-21.
- 2026-07-21: Phase 6 — verify vòng 1: 8/8 tiêu chí phase-6.md có bằng chứng đạt (2 biểu đồ, ẩn số tiền, cảnh báo tập trung + A2 + hysteresis + chú thích liên kết, skeleton/Suspense); chặn bởi bug `formatMonthYear` (lib/format.ts) sai định dạng, phát hiện qua 3 unit test mới viết (format.test.ts).
- 2026-07-21: Phase 6 — verify vòng 2: `design-implementer` sửa `formatMonthYear` (cắt chuỗi từ `formatDate()` thay vì `Intl.DateTimeFormat` riêng, giống kỹ thuật `formatDayMonth`), 3 test pass (241/241 tổng); không phát sinh gì mới. **Phase 6 hoàn thành** ✅ — còn 2 việc hạ tầng chưa verify thật trên Claude Cloud (e2e Playwright cần Docker; `prisma migrate dev` thật cho migration `20260721090159_add_holding_concentration_warning_active`), cần chạy lại trên Claude Local trước khi hoàn toàn yên tâm.
- 2026-07-24: Phase 6 — issue #67: tách realized/unrealized PnL trên card Dashboard.
- 2026-07-24: fix code review PR #87 (issue #83/#82/#67): sửa `realizedPnl` sai khi holding có cổ tức cổ phiếu (2 bộ đếm song song, `lib/realized-pnl.ts`), thêm cờ `pnlSplitIsApproximate` cảnh báo cutoff-approximate, dọn trùng lặp `sortByPositionTrailOrder`/`paginateWithCursor`. Xem `DECISION.md` 2026-07-24 (2).
- 2026-07-24: sửa lần 2 PR #87 — retrofit thiết kế "2 bộ đếm song song" thành "1 bộ đếm `realQuantity`" cho `computeRealizedGainForHolding` (ca bán một phần rồi mua tiếp trước đó tính sai) VÀ fix cùng họ bug ở write-path `derivePositionIncludingStockDividends` (`lib/cost-basis.ts`, chưa từng fix). Xem `DECISION.md` 2026-07-24 (3).
- 2026-07-24: sửa lần 3 PR #87 — xoá `derivePosition()` cũ (chỉ-Cashflow, không còn production caller sau sửa lần 2), đổi tên `derivePositionIncludingStockDividends()` thành `derivePosition()` (chiếm lại tên, giờ là cài đặt DUY NHẤT), gộp 10 test case cũ vào test suite hàm mới với `stockDividends=[]`. Gộp test lộ ra bug thật (sửa lần 4): `avgCost` không reset về 0 khi đóng hết vị thế bằng SELL không có BUY sau đó — thêm reset tường minh dựa trên `quantity` thật. Xem `DECISION.md` 2026-07-24 (4).
- 2026-07-26: Phase 7 — verify issue #56 (PR #102): `BondTerms`/`BondIssuerType`/`DividendType.BOND_COUPON`/`CashflowType.MATURITY`/`Dividend.parValueApplied`+`couponRatePercentApplied`/2 Setting `BOND_INTEREST_TAX_RATE_*` đúng như phase-7.md mục 1, docs domain đồng bộ, không sót duplicate declaration ở 11 file conflict cũ; bổ sung 4 unit test (MATURITY ở `computeCashflowAmount`/`derivePosition`/`computeRealizedGainForHolding`) vì phase-7.md yêu cầu tường minh "xác nhận bằng test, không sửa mù". Tick mục 1 "Schema & Setting" trong `phase-7.md`; Phase 7 vẫn 🟨 (còn #57/#58/#101) và migration thật (`prisma migrate dev`) còn chờ chạy trên Claude Local.
- 2026-07-26: issue #105 (sub-issue #104, làm ĐẦU TIÊN trong chuỗi refactor clean-code): gộp 3 ca lặp tri thức nguy hiểm nhất. (a) `PENDING_EVENT_CREATED_AT` (`lib/position-trail.ts`) thay 2 hằng số `CANDIDATE_CREATED_AT`/`PROBE_CREATED_AT`. (b) `cashflowPositionDelta()`/`dividendPositionDelta()` + `buildPositionEvents()` (switch + `assertNever`, `lib/position-trail.ts`) — `recordDividend`/`getDividendHistory` không còn tự dựng `PositionTrailEvent[]` tay; `derivePosition()` dùng lại `cashflowPositionDelta()`. (c) `positionSourceSelect` (`lib/position-trail.ts`) gộp khối `where: {type: STOCK} + select` cashflow/dividend, dùng ở cả 4 action ghi giao dịch + `getHoldingDetail`. `cost-basis.test.ts` pass nguyên trạng không sửa; `position-trail.test.ts` chỉ đổi 1 literal trùng giá trị hằng số sang import constant (để `grep "8640000000000000" src/` còn đúng 1 kết quả), không đổi assertion nào. `pnpm typecheck && pnpm lint && pnpm test` sạch (278/278). `pnpm e2e` CHƯA chạy được (Claude Cloud, cần Docker) — theo `TOOLS.md`/issue #104, bắt buộc verify lại trên Claude Local trước khi merge. PR #112, merge vào `claude/issue-99-phase-7-sdr0ds`.
- 2026-07-26: issue #106 (sub-issue #104, sau #105): dựng `features/holdings/repository.ts` + `features/dividends/repository.ts` — nơi DUY NHẤT của mỗi feature được `import { db }`/gọi `tx.*`; `actions.ts`/`queries.ts` không còn chạm Prisma trực tiếp. `findPositionSourceById/BySymbol/ByCashflow` (holdings) + `findDividendPositionSource` (dividends) gộp select + mapper + check `userId` từng lặp 5 nơi 5 câu lỗi khác nhau thành 1 chỗ; "không tồn tại"/"không thuộc user" đều trả `null`. `tx: Prisma.TransactionClient = db` (đã verify bằng `pnpm typecheck` trước khi viết loạt — `db` gán được vào kiểu đó); `runInTransaction()` (pass-through mỏng cho `db.$transaction`, khai trong mỗi repository.ts) để `actions.ts` không cần tự `import { db }` mà vẫn là nơi quyết định ranh giới/isolation level transaction. Hardening thêm ngoài đề xuất gốc của issue: 3 hàm batch theo `holdingIds[]` (`findCashflowsForHoldings`/`findCashDividendsForHoldings`/`findCashflowDateRangeForHoldings`) và `findCashLikeDividendNetAmounts` trước đây tin caller đã lọc `userId` sẵn (comment cũ) — nay tự thêm filter `holding: { userId }`, không dựa vào caller nữa. `grep -rn "@/lib/db" src/features/holdings src/features/dividends` chỉ khớp 2 file `repository.ts`; `grep "userId !=="` không còn trong `actions.ts`/`queries.ts` của 2 feature. **Phạm vi cố ý KHÔNG gồm** `lib/*.ts` (settings.ts, valuation.ts, portfolio-valuation.ts, auth.ts...) và `features/{settings,snapshots,members}` — đúng "Khu vực cần refactor" nguyên văn của issue #106 (`repository.ts` cho 3 feature đó "thêm khi chạm tới", để việc sau). `pnpm typecheck && pnpm lint && pnpm test && pnpm build` sạch (278/278 unit test, build production pass). `pnpm e2e` CHƯA chạy được (Claude Cloud) — bắt buộc verify trên Claude Local trước khi merge (rủi ro cao nhất chuỗi refactor: rò dữ liệu giữa user).
- 2026-07-28: Phase 7 — issue #58 (trái tức) + #101 (tất toán đáo hạn), lớp Server Action: `computeBondCoupon()` + `lib/bond-schedule.ts` (suy kỳ trả lãi tới, dùng chung với Phase 8) + nhánh `BOND_COUPON` trong `recordDividend` (đóng băng `parValueApplied`/`couponRatePercentApplied`, KHÔNG bù pha loãng NAV) + `settleMaturity` ghi `Cashflow{MATURITY}` (`lib/maturity-settlement.ts`); trái tức vào chuỗi XIRR qua hằng số dùng chung `CASH_FLOW_DIVIDEND_TYPES`; quy ước thứ tự Dividend-trước-MATURITY cùng ngày cài bằng `rank` thắng `createdAt`. Wiring 3 route mới (`/bond-terms`, `/maturity`, tab "Trái tức" ở form ghi cổ tức) — 7a đảo thành route riêng, xem `DECISION.md` 2026-07-28 (2). Vá thêm lỗ hổng phát hiện khi rà: sửa một dòng `MATURITY` qua form giao dịch có thể đổi loại sang `SELL` và nhảy sang chế độ thuế khác. `pnpm lint/typecheck/test/build` sạch (374/374, +52 test); `prisma migrate dev` và `pnpm e2e` vẫn CHƯA chạy được trên Claude Cloud — xem `phase-7.md` mục "Trạng thái verify".
- 2026-07-28: sửa #58 sau phản biện của user ("9% là số cố định à?") — bỏ cách suy ngược kỳ trả lãi từ `grossAmount`, thay bằng cột đóng băng `Dividend.couponFrequencyMonthsApplied`. Mẫu số của phép đảo chứa SL-tại-ngày-ghi (derive lại mỗi lần đọc), nên sửa/xoá một lệnh mua cũ sẽ đổi nhãn kỳ đã ghi từ `kỳ 6 tháng` sang `kỳ 4`/`kỳ 12` — sai âm thầm. Lý do "tránh thêm migration" cũng sai: chưa có migration nào cho `BondTerms`. Xoá hẳn `deriveCouponFrequencyMonths()` + 5 test thay vì giữ làm fallback. `lint/typecheck/test/build` sạch (369/369). Xem `DECISION.md` 2026-07-28 (3).
- 2026-07-30: Phase 7 — verify hạ tầng còn treo trên Claude Local: `prisma migrate dev` thật đã áp migration `20260729233213_add_bond_terms_and_coupon_maturity`; `pnpm e2e` 43/43 (2 kịch bản bắt buộc trái tức/đáo hạn mới). Phát hiện + sửa bug thật khi chạy e2e: `recordDividend()` không coerce `percent` (`null` → `undefined`) nên mọi lần ghi trái tức qua UI bị zod từ chối, ngoài tầm 372 unit test cũ (commit `3875282`). Grep `userId`/`Decimal` cho code Phase 7 sạch. **Phase 7 hoàn thành** ✅.
- 2026-08-08: bổ sung sau Phase 7 (ngoài phase, không gắn tiêu chí): override thủ công `grossAmount` cho trái tức (`BOND_COUPON`) theo đúng cơ chế `taxAmount` đã có, thêm `Dividend.grossAmountOverridden` (audit cờ, không có tiền lệ ở `taxAmount` — lý do xem `DECISION.md` 2026-08-08); guard `taxAmount > grossAmount` đổi so với `grossAmount` CUỐI CÙNG (đã qua override) thay vì số tự tính. `pnpm typecheck/lint/vitest/build` sạch (382/382, +2 test refine mới ở `schemas.test.ts`); `prisma migrate status` xác nhận đã áp migration trên DB local. `pnpm e2e` CHƯA chạy (user chủ động bỏ qua lượt này, hết usage AI) — TODO ghi trong PR, chưa verify thật luồng override qua UI.
- 2026-08-13: đóng 3 việc còn treo ở `DECISION.md` (bonds, 2026-07-29/2026-08-08), ngoài phase: (1) gộp `getAllCashDividendsForXirr()` (`lib/portfolio-valuation.ts`, tự viết `db.dividend.findMany()` KHÔNG filter `userId` trực tiếp) về `findCashDividendsForHoldings()` (`features/holdings/repository.ts`, đã có `holding: { userId }`) — `computeXirrCore()` nhận thêm tham số `userId`, thêm `taxAmount` vào `CashDividendForXirrRow` để `computeCostDrag()` vẫn tính đúng "chi phí ăn mòn"; (2) `recordDividendSchema` (`features/dividends/schemas.ts`) đổi từ 1 object + refine sang `z.discriminatedUnion("type", [cashDividendSchema, stockDividendSchema, bondCouponDividendSchema])`, mỗi variant `z.strictObject` (field lạ vẫn bị từ chối đúng như 2 refine cũ, không âm thầm bỏ qua) — xoá hẳn non-null assertion `percent!` ở `recordDividend()` (actions.ts: `CashDividendResult`/`StockDividendResult` giờ tự mang `percent` thay vì actions.ts phải ghép lại từ input sau khi transaction xong); phát hiện + vá thêm 1 lỗ hổng message phát sinh khi làm: field decimal bắt buộc bị bỏ trống hoàn toàn (không phải nhập sai) rơi vào message tiếng Anh mặc định của zod thay vì message tiếng Việt đã truyền — sửa `decimalString()` (`features/holdings/schemas.ts`, dùng chung toàn app) truyền `error: message` vào `z.string()`, cải thiện luôn cho mọi field decimal bắt buộc khác (quantity/price/parValue...), không chỉ riêng dividends; (3) chạy `pnpm e2e` lần đầu tiên trên Claude Cloud (Postgres native, hạ tầng mới mở theo quyết định 2026-08-13 ở `agent-workflow-and-tooling.md`) cho `bond-coupon-and-maturity.spec.ts` — cả 4 test (2 kịch bản bắt buộc từ Phase 7 + 2 kịch bản override gross từ 2026-08-08, hoá ra đã viết sẵn từ trước, chỉ chưa từng chạy được) PASS; chạy tiếp toàn bộ suite (45/45 PASS) để xác nhận đổi schema dùng chung không phá luồng CASH/STOCK khác. `pnpm lint/typecheck/test` sạch (382/382) trước khi chạy e2e.
- 2026-08-13: fix(dividends) bug ngoài phase: `avgCost` không dilute qua cổ tức cổ phiếu (`Dividend{type: STOCK}`), gây `realizedPnl` tính thấp hơn thực tế khi bán sau khi nhận cổ tức CP — sửa công thức đóng đồng bộ ở `derivePosition()` (`lib/cost-basis.ts`), `computeRealizedGainForHolding()` (`lib/realized-pnl.ts`), `recordStockDividend()` (`features/dividends/actions.ts`); thêm script backfill dry-run-by-default (`scripts/backfill-stock-dividend-avgcost.ts`, `--apply` CHƯA chạy lên production — để user tự làm sau merge); docs domain + `DECISION.md`/`decisions/dividends.md` đồng bộ (đảo một phần entry 2026-07-16). `pnpm lint/typecheck/test` 382/382 pass, `pnpm e2e` 45/45 pass (13 test `dividends.spec.ts`). Verify: đối chiếu diff `4cbfa97` thật khớp 2 báo cáo, grep `userId`/`Decimal` sạch trên các file sửa, không cần thêm unit test (bộ số hand-computed đã đủ). Không thuộc `phase-x.md` nào — không tick tiêu chí phase.
- 2026-08-16: issue #131 (ngoài phase) — Presentational `StockAllocationDetail` (tỉ trọng theo mã trong nhóm cổ phiếu, mẫu số khác `ConcentrationBadge` toàn danh mục) + `StockAllocationRow`/`InfoCallout`/`SortToggleButton`/skeleton + preview `stock-allocation-detail`, dựng từ digest `process/UI_stock-allocation-detail.md`. Chốt route riêng (không accordion) — xem `DECISION.md` 2026-08-16. Chỉ lớp UI, chưa có query/route thật (#132). Không thuộc `phase-x.md` nào — không tick tiêu chí phase.
- 2026-08-16: issue #132 (ngoài phase, tiếp nối #131) — wiring dữ liệu thật: `computeStockGroupAllocation()` (`lib/stock-group-allocation.ts`, pure, đã có unit test từ commit trước) + `getStockAllocationDetail()` mới (`lib/portfolio-valuation.ts`, tái dùng `getConcentrationBadges()` cho badge amber cạnh tên mã) + route `/allocation/stock` (`page.tsx`/`loading.tsx`) + `StockAllocationDetailClient` (client wrapper giữ state `hidden`/sort cục bộ, format tiền theo cờ ẩn — mirror `DashboardScreenClient`) + đổi dòng legend "Cổ phiếu" trong `AllocationScreen.tsx` thành `<Link>` trỏ route mới (các nhóm tài sản khác vẫn `<div>` tĩnh). Đóng "Việc còn treo" ghi ở `DECISION.md` 2026-08-16 (entry #131). Đóng luôn issue #134 (duplicate 100% của #132, tạo cùng ngày cách #132 ~1.5h) theo yêu cầu user, comment giải thích + `state_reason: duplicate`. `pnpm lint/typecheck/test/build` sạch (387/387 unit test); thêm 1 e2e case mới (`concentration-and-allocation.spec.ts`: 2 mã có giá + 1 mã thiếu giá → đúng % mẫu số nhóm cổ phiếu, cập nhật giá xong tính lại đúng, sort toggle) + `AllocationStockPage` page object (`e2e/pages/`); gặp + ghi `GOTCHAS.md` #21 (2 số % khác mẫu số trùng giá trị khi danh mục test chỉ toàn STOCK, `strict mode violation`, fix bằng `{exact: true}`). Không thuộc `phase-x.md` nào — không tick tiêu chí phase.
- 2026-08-19: issue #142 (ngoài phase) — `createHolding()` thêm `intent: "HISTORICAL" | "NEW_PURCHASE"` bắt buộc: nhánh `NEW_PURCHASE` (vừa mua mới thật) tính `avgCost` theo đúng công thức BUY chuẩn có phí (`TRANSACTION_FEE_BUY_<LOẠI>`, cùng cơ chế `TransactionForm`), chặn ngày tương lai, và báo lỗi khi trùng mã (đọc số liệu từ cache `Holding` đã materialize, không replay `derivePosition()`) thay vì gộp im lặng như nhánh `HISTORICAL` (giữ nguyên hành vi cũ). Sửa cost basis + XIRR cho đúng ngay từ giao dịch đầu tiên của vị thế. Xem `DECISION.md` 2026-08-19.
