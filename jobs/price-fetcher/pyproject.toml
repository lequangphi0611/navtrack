[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
markers = ["integration: can Postgres that (docker-compose.test.yml) - xem docs/rules/python-job.md"]
addopts = "-m 'not integration'"
